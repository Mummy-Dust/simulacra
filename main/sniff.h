#pragma once
// Wi-Fi-only verification/seed mode: promiscuous-capture probe requests and log counts over
// serial (confirms M7 injection; also the seed for a future Wi-Fi observe milestone).
void sniff_start(void);
